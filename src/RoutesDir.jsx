import {useLocation} from "react-router-dom"

const listaDePaginas = ["/login", "/registrar-usuario", "/recuperar-contrasenia"]

const RoutesDir = () => {
    const location = useLocation()
}

export default RoutesDir;